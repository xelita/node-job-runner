{
  "name": "node-scheduler",
  "version": "0.1.0",
  "description": "node-scheduler allows scripts to be executed every defined amount of time",
  "main": "src/app.js",
  "scripts": {
    "clean": "gulp clean",
    "build": "gulp",
    "start": "node dist/app.min.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "scheduler",
    "cron"
  ],
  "author": "xelita <dev@xelita.com>",
  "devDependencies": {
    "babel-preset-es2015": "^6.14.0",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-clean": "^0.3.2",
    "gulp-concat": "^2.6.0",
    "gulp-print": "^2.0.1",
    "gulp-uglify": "^2.0.0"
  }
}
